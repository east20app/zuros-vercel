"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { saveRequiredDiscordId } from "@/lib/actions/identity.actions";
export default function RequiredDiscordPage() { const router = useRouter(); const [value, setValue] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); setLoading(true); setError(""); try { await saveRequiredDiscordId(value); router.replace("/dashboard"); router.refresh(); } catch (e) { setError(e instanceof Error ? e.message : "Não foi possível salvar."); } finally { setLoading(false); } } return <main className="required-discord-page"><section><p className="home-kicker">ETAPA OBRIGATÓRIA</p><h1>Informe seu ID do Discord</h1><p>Para continuar usando a plataforma, precisamos vincular seu usuário Discord à sua conta criada por e-mail.</p><form onSubmit={submit}><label>ID do usuário Discord<input required inputMode="numeric" value={value} onChange={(e) => setValue(e.target.value)} placeholder="Ex.: 123456789012345678" /></label><small>Ative o Modo Desenvolvedor no Discord, clique no seu perfil e escolha “Copiar ID do usuário”.</small><button disabled={loading}>{loading ? "Salvando..." : "Continuar para o dashboard"}</button>{error && <p className="support-notice">{error}</p>}</form></section></main>; }

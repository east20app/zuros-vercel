import Image from "next/image";
import { listApplicationDiscordMembers } from "@/lib/actions/discord-members.actions";
import { requireUser } from "@/lib/require-admin";
import { ActionError } from "@/lib/actions/context";
import { notFound } from "next/navigation";
export const dynamic = "force-dynamic";
export default async function ApplicationUsersPage({ params }: { params: Promise<{ appId: string }> }) { await requireUser(); const { appId } = await params; let data; try { data = await listApplicationDiscordMembers(appId); } catch (error) { if (error instanceof ActionError) notFound(); throw error; } return <main className="mx-auto max-w-6xl px-5 py-8"><header className="bot-page-hero"><p className="home-section-index">APLICAÇÃO / DISCORD</p><h1>Usuários do servidor</h1><p>{data.applicationName} · {data.members.length} usuário(s) retornado(s) pelo Discord.</p></header><section className="sales-summary"><article className="sales-pending-stat"><span>USUÁRIOS REAIS</span><strong>{data.members.length}</strong><small>Consulta direta ao servidor</small></article><article className="sales-pending-stat"><span>ROBÔS</span><strong>{data.members.filter((member) => member.bot).length}</strong><small>Contas automatizadas</small></article><article className="sales-pending-stat"><span>PESSOAS</span><strong>{data.members.filter((member) => !member.bot).length}</strong><small>Membros humanos</small></article></section>{data.message && <p className="support-notice">{data.message}</p>}{data.members.length > 0 && <section className="card mt-6 overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead><tr className="border-b border-zinc-800 text-xs uppercase tracking-wide text-zinc-500"><th className="p-4">Usuário</th><th className="p-4">ID Discord</th><th className="p-4">Tipo</th><th className="p-4">Entrada no servidor</th><th className="p-4">Cargos</th></tr></thead><tbody>{data.members.map((member) => <tr key={member.id} className="border-b border-zinc-900 text-zinc-300"><td className="p-4"><span className="flex items-center gap-3">{member.avatar ? <Image src={member.avatar} alt="" width={32} height={32} className="h-8 w-8 rounded-full" unoptimized /> : <span className="flex h-8 w-8 items-center justify-center rounded-full bg-blue-600/20 text-blue-300">{member.displayName.slice(0, 1).toUpperCase()}</span>}<span><strong>{member.displayName}</strong><small className="block text-zinc-500">@{member.username}</small></span></span></td><td className="p-4 font-mono text-xs text-zinc-400">{member.id}</td><td className="p-4">{member.bot ? "Bot" : "Usuário"}</td><td className="p-4">{member.joinedAt ? new Date(member.joinedAt).toLocaleDateString("pt-BR") : "—"}</td><td className="p-4">{member.roles.length || "—"}</td></tr>)}</tbody></table></div></section>}</main>; }
